import { FilterNomPipe } from './filter-nom.pipe';

describe('FilterNomPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNomPipe();
    expect(pipe).toBeTruthy();
  });
});
